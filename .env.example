# Copy this file to .env and fill in your own values.

# Local MongoDB:
MONGODB_URI=mongodb://127.0.0.1:27017/periphtrack

# Or MongoDB Atlas (cloud), looks like:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/periphtrack?retryWrites=true&w=majority

PORT=4000
